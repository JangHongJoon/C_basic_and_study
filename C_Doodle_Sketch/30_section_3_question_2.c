// * 다시 한 번 복습 시급합니다. 

//1. 
 /*
 	주소 값
	주소 값
	주소 값
	주소 값
	주소 값
	0
	주소 값
	0
	
	[풀이]
	&arr == arr == *arr 다 같이 주소값을 출력
	
	&arr[0] == arr[0] --> 둘 다 배열에 대한 주소 값
	*arr[0] --> 주소 값에 * 을 붙혀서 저장 값을 출력
	
	&arr[0][0] ==> 0,0 의 주소 값을 출력
	arr[0][0] --> 0,0 에 저장값 출력
	 
 */
 
 //2.
 /*
 	주소 값
	주소 값 + 4
	주소 값 + 12
	주소 값 + 12
	주소 값 + 36     
 */
