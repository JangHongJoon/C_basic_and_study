// RAM -> 저장공간에 공간을 뽑아서 4 라는 값을 저장하면
		   접근 하는 것
		   
#include <stdio.h>
int main()
{
	int a; // int -> 변수의 형태 중 하나 
				// 32bit(4byte) RAM 에 할당 
			// a 변수 선언		
	
	a=3; // a에 3을 대입 
	printf("%d\n",a); 
	
	a=5;
	printf("%d\n",a); 
	
	// 변수 이름 : 알파벳 대소문자, _, 숫자 (첫번째 글자 X)
	 
	
 } 
